# ---------- ПРОМПТ ----------

PROMPT_TEMPLATE = """\
Роль: Ты — система анализа изометрических чертежей трубопроводов.
Ты работаешь с координатами вершин и координатами размерных чисел,
извлечёнными из чертежа. Изображения тебе недоступны — работай
с числовыми данными и логикой.

=== ВХОДНЫЕ ДАННЫЕ ===

Вершины (id, роль, цвет, координаты, степень):
{VERTICES_BLOCK}

Размерные числа (значение, координаты центра):
{NUMBERS_BLOCK}

Семантика:
  endpoint (красный, deg=1) — конец трубопровода
  corner   (зелёный, deg=2) — угол
  junction (синий,   deg=3) — ответвление

=== ЗАДАЧА ===

Восстановить основную цепочку трубопровода, выделить ответвления
и привязать каждое число к отрезку между двумя вершинами.

=== КЛЮЧЕВЫЕ ПРАВИЛА ===

1. ПОСТРОЕНИЕ ГРАФА:
   Две вершины соединены, если между ними на чертеже проходит отрезок
   трубы. Используй координаты: расстояние между соседними вершинами
   должно быть минимальным, а линия — прямой или с одним изломом.

2. ОСНОВНАЯ ЦЕПОЧКА:
   Самый длинный путь между двумя endpoint (deg=1), проходящий
   через ВСЕ corner и ВСЕ junction.
   - junction НЕ может быть концом основной цепочки.
   - junction — точка ветвления: от него уходит ответвление,
     а сама цепочка ПРОДОЛЖАЕТСЯ через него.

3. ОТВЕТВЛЕНИЕ:
   Путь от junction до endpoint, не входящий в основную цепочку.

4. ПРИВЯЗКА ЧИСЕЛ:
   Каждое число — это размерная линия между двумя точками.
   - Если ОБА конца линии совпадают с отмеченными вершинами
     -> число привязывается к отрезку между ними.
   - Если ХОТЯ БЫ ОДИН конец не совпадает с отмеченной вершиной
     -> число скипается с причиной "second_endpoint_not_marked_as_vertex".
   - Число-сосед, у которого оба конца привязаны к вершинам, остаётся
     на том же отрезке и указывается в kept_sibling у скипнутого.

5. ПРОВЕРКИ:
   - endpoint не может быть в середине цепочки.
   - junction соединяет ровно 3 отрезка.
   - corner соединяет ровно 2 отрезка.
   - На одном отрезке не может быть двух чисел с обоими
     привязанными концами.
   - КАЖДОЕ число из входного списка должно попасть
     либо в segments, либо в skipped.

=== ПОДСКАЗКА ПО ГЕОМЕТРИИ ===

Используй координаты для определения связей. Например:
- Ближайшая вершина к V01 по расстоянию — вероятный сосед.
- Если между двумя вершинами есть третья, лежащая на той же прямой, —
  значит, они не соседи, а соединены через неё.
- Ответвление обычно короткое и уходит в сторону от основной линии.

=== ФОРМАТ ВЫВОДА (JSON) ===

{{
  "main_chain": {{
    "start": "Vxx",
    "end": "Vyy",
    "path": ["V01", "V04", ...],
    "segments": [
      {{
        "from": "V01",
        "to": "V04",
        "value": 60,
        "coords": [638.7, 53.5]
      }}
    ]
  }},
  "branches": [
    {{
      "junction": "Vxx",
      "endpoint": "Vyy",
      "path": ["Vxx", "Vyy"],
      "segments": [
        {{
          "from": "Vxx",
          "to": "Vyy",
          "value": 34,
          "coords": [337.0, 202.5]
        }}
      ]
    }}
  ],
  "skipped": [
    {{
      "value": 916,
      "coords": [555.0, 161.7],
      "attached_to": "V04",
      "reason": "second_endpoint_not_marked_as_vertex",
      "kept_sibling": {{
        "value": 1950,
        "coords": [487.6, 220.4]
      }}
    }}
  ]
}}

Верни ТОЛЬКО JSON, без пояснений и без markdown-обёртки.
"""


# ---------- ОТПРАВКА В DEEPSEEK ----------